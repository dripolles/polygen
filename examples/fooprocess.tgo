{{ $a := T "a" }}
{{ $idA := Id "a" }}

func (f * Foo) process{{$idA}}(xs []{{$a}}, init, max {{$a}}) ({{$a}}, error) {
    res := init
    for _,x := range xs {
        res += x
        if res > max {
            return res, errors.New("Over max")
        }
    }

    return res, nil
}
