func (f * Foo) process{{Id "a"}}(xs []{{T "a"}}, init, max {{T "a"}}) ({{T "a"}}, error) {
    res := init
    for _,x := range xs {
        res += x
        if res > max {
            return res, errors.New("Over max")
        }
    }

    return res, nil
}
